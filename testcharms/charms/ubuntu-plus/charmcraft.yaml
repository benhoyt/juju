name: ubuntu-plus
summary: "A ubuntu charm with a dispatch hook and a few actions."
description: |
    This is a longer description which
    potentially contains multiple lines.
type: "charm"
base: ubuntu@24.04
platforms:
  amd64:
  arm64:
  s390x:
parts:
  # Include extra files in the packed charm
  include:
    plugin: dump
    source: .
    prime:
      - actions
      - hooks
      - tools
      - dispatch
      - revision
      - version
